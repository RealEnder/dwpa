* add current round stats
* use stdout instead of writting temp file in tshark execution
* add key with submissions
* keep key in cookie, add to interface
* show passwords with key only
* rewrite texts
* use shm insted of /tmp
* windows version of help_crack
* graf stats
* more stats
* write INSTALL instructions
* write Android app with net scan and pass show from the site
